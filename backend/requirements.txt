# --- FastAPI / web stack ----------------------------------------------------
fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
python-multipart>=0.0.20
websockets>=14,<16
pydantic-settings>=2.7,<3
httpx>=0.28,<1
psycopg[binary]>=3.2,<4
python-kasa>=0.7,<1
tapo>=0.8,<1
tinytuya>=1.13,<2
pywemo>=0.10,<1
somecomfort>=0.8,<1
meross-iot>=0.4,<1

# --- Optional local-first smart-light / discovery drivers -------------------
# All imported lazily; a missing wheel degrades to a clear "not installed"
# message and never crashes discovery or control. See
# roomos/devices/discovery.py and roomos/devices/lights_control.py.
zeroconf>=0.131,<1      # mDNS discovery (Shelly, Nanoleaf, Hue)
pywizlight>=0.5,<1      # WiZ bulbs (UDP)
yeelight>=0.7,<1        # Yeelight bulbs (LAN)
aiolifx>=1.0,<2         # LIFX bulbs (UDP)
nanoleafapi>=2.1,<3     # Nanoleaf panels (token)
python-dotenv>=1.0,<2
openai>=1.40,<2
python-telegram-bot>=21,<22

# --- CLI / config -----------------------------------------------------------
# typer 0.25+ needs click 8.2+ (subscriptable Choice); pin explicitly so
# transitive deps (e.g. aiolifx) cannot leave click 8.1.x and break scripts.
click>=8.2,<9
typer>=0.12,<1
pyyaml>=6.0.1,<7
rich>=13.7,<15

# --- Data / numerics --------------------------------------------------------
numpy>=1.26,<3
pandas>=2.1,<3
scikit-learn>=1.4,<2
joblib>=1.3,<2

# --- ML ---------------------------------------------------------------------
xgboost>=2.0,<3

# --- Vision / perception ----------------------------------------------------
# Pinned to "compatible" ranges; the heavy deps (torch, mediapipe) are
# intentionally loose so users can pick wheels for their platform / Python.
opencv-python>=4.9,<5
# torch is intentionally not pinned to a CUDA build; install per platform.
torch>=2.2
open_clip_torch>=2.24,<3
# mediapipe ships limited wheels on some platforms. The pipeline degrades
# gracefully if mediapipe is unavailable; see roomos/features/pose_extractor.py.
mediapipe>=0.10.14 ; python_version < "3.13"

# --- Visualization / debug --------------------------------------------------
matplotlib>=3.8,<4
seaborn>=0.13,<1

# --- Testing ----------------------------------------------------------------
pytest>=8.0,<9
