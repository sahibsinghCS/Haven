# ARCHIVED — not part of the hackathon demo

This folder is an early **Vite + React scratch UI**. It is **not** started by `npm run demo` or `npm run dev`.

**Use instead:** [`web/`](../web/) (Next.js) — routes `/live` and `/preferences`.

Do not delete without team agreement; kept as reference for shadcn component experiments only.
